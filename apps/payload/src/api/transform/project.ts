import type * as API from '@monolithe/api/types'
import type { Project, PageSlug } from '@/types'

export const transformProjectData = ({}: Project, slug: PageSlug): API.Project.Data => {
  return {}
}
