---
import HeroView from '@/components/HeroPlant.astro'

import type { Hero } from '@monolithe/api/types'

type Props = {
  hero: Hero
}

const { hero } = Astro.props
---

<div class="page">
  <HeroView {...hero} />
</div>
